spring:
  application:
    name: eureka-producer-feign-hystrix
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # 指定 Eureka 注册中心的地址
server:
  port: 9000
feign:
  hystrix:
    enabled: true